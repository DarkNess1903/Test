const band ={
    member: 10,
    name: 'GGBananaBro',
    popularSong: 'Monkey eats banana song',
};
function bandDetail(){
        // return console.log(band.name+band.member+band.popularSong);
        return console.log(band.name,band.member,band.popularSong);
}
bandDetail();